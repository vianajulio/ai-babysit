"""Tools MCP do Babysit, agrupadas por assunto.

`mcp_server_standalone.py` é só a fachada: importa este pacote (o que
registra as tools) e roda o servidor. A divisão existe porque o arquivo
único passou do limite de tamanho do próprio quality gate.
"""
